# ═══════════════════════════════════════════════════════════════
# Modulys Pax — Client Backend (backend do app do tenant)
# Gerado por tenant; preencher PAX_TENANT_CODE e URLs conforme ambiente.
# ═══════════════════════════════════════════════════════════════

PORT=4000

# URL da Admin API (tenant-auth/login e tenant-auth/me)
PAX_ADMIN_API_URL=http://localhost:3000/api/admin
# Código do tenant (ex: my-company). Preenchido na geração do projeto.
PAX_TENANT_CODE=__TENANT_CODE__

# Mesmo valor da Admin API — obrigatório para validar o JWT do tenant
PAX_JWT_SECRET=modulys-pax-jwt-secret-dev-change-in-production

# URLs dos serviços Core e Chat (para proxy /api/core e /api/chat)
PAX_CORE_SERVICE_URL=http://localhost:9002
PAX_CHAT_SERVICE_URL=http://localhost:9001
# Mesmo valor da Admin API — obrigatório para o Core aceitar as requisições do proxy
PAX_SERVICE_KEY=modulys-pax-internal-service-key-dev-change-in-production
