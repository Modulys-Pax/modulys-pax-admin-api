# ═══════════════════════════════════════════════════════════════
# Modulys Pax — Client Frontend (Next.js / app do tenant)
# Apontar para o client-backend e para o Chat (WebSocket).
# ═══════════════════════════════════════════════════════════════

# URL do backend do cliente (login, /auth/me, proxy /api/core e /api/chat)
NEXT_PUBLIC_API_URL=http://localhost:4000

# Chat em tempo real (Socket.IO). URL do modulys-pax-chat-service
NEXT_PUBLIC_CHAT_WS_URL=http://localhost:9001
